- FIXED: child doesnt have a parent.
- When process exits and calls goto_next_process, we segfault when we about to "pop a molly" to process 0.
        Nothing has yet been freed in kernel_exit.
- Child thinks it is its own child.
